name: Release Golang executable on tag

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"
  workflow_call:
    inputs:
      tag:
        required: true
        type: string
      release_body:
        description: >-
          Release notes. Left empty (the default), the CHANGELOG section for the
          tag is used instead. Callers that cannot add a CHANGELOG entry - a bot
          commit cannot land on protected main - pass their own text here.
        required: false
        type: string
        default: ""

env:
  TAG: ${{ inputs.tag != '' && inputs.tag || github.ref_name }}

jobs:
  release-prerequisites:
    runs-on: ubuntu-latest
    outputs:
      RELEASE_BODY: ${{ inputs.release_body != '' && inputs.release_body || steps.release-prerequisites.outputs.RELEASE_BODY }}
    steps:
      # The action's only job is to find the CHANGELOG section for the tag, so it
      # is skipped when the caller already supplied the body.
      - id: release-prerequisites
        if: ${{ inputs.release_body == '' }}
        uses: thetillhoff/action-release-prerequisites@2c4e295d06d4b8ce6fe2bdf6a1b7e806d7c1687c # v0.4.1
        with:
          RELEASE_TAG: ${{ env.TAG }}

  golang-build:
    needs: release-prerequisites
    runs-on: ubuntu-latest
    permissions:
      contents: write
    strategy:
      matrix:
        os:
          - linux
          - windows
          - darwin
        arch:
          - amd64
          - arm64
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: thetillhoff/action-golang-build@b94bf8d46bc2dd0f7324fcbd0c6530838943976e # v1.1.0
        with:
          OS: "${{ matrix.os }}"
          ARCH: "${{ matrix.arch }}"
          BUILDARGS: -ldflags="-X main.version=${{ env.TAG }}"
          RELATIVE_PACKAGE_PATH: ./cmd/webscan/

  verify-version:
    needs: golang-build
    runs-on: ubuntu-latest
    steps:
      - name: Download linux/amd64 artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: artifact_webscan_linux_amd64
          path: .

      - name: Verify version
        shell: bash
        run: |
          chmod +x ./webscan_linux_amd64

          # Check version using --version flag
          VERSION_OUTPUT=$(./webscan_linux_amd64 --version 2>&1 || echo "dev")
          VERSION_OUTPUT=$(echo "$VERSION_OUTPUT" | tr -d '\n\r' | xargs)  # Trim whitespace and newlines

          echo "Version output: '$VERSION_OUTPUT'"

          # Verify version matches the tag (this also catches "dev" since it won't match)
          EXPECTED_VERSION="${{ env.TAG }}"
          if [[ "$VERSION_OUTPUT" != "$EXPECTED_VERSION" ]]; then
            echo "ERROR: Version mismatch! Expected: '$EXPECTED_VERSION', Got: '$VERSION_OUTPUT'"
            exit 1
          fi

          echo "✓ Version verification passed: $VERSION_OUTPUT"

  cleanup-on-failure:
    needs:
      - release-prerequisites
      - golang-build
      - verify-version
    if: always() && (needs.release-prerequisites.result == 'failure' || needs.golang-build.result == 'failure' || needs.verify-version.result == 'failure')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: Delete tag on failure
        shell: bash
        run: |
          git config --global user.name 'GithubActions'
          git config --global user.email 'githubactions@users.noreply.github.com'
          git push --delete origin "${{ env.TAG }}"
          echo "Deleted tag: ${{ env.TAG }}"

  release-artifacts:
    needs:
      - golang-build
      - verify-version
      - release-prerequisites
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          merge-multiple: true

      # tag_name is mandatory here: the action falls back to github.ref, which is
      # refs/heads/main when tag-on-main calls this workflow, and it refuses to
      # release without a tag ref ("GitHub Releases requires a tag").
      - name: Release
        uses: softprops/action-gh-release@efb35369e0ad2afab669f228072c1b0d510eae64 # v3
        with:
          tag_name: ${{ env.TAG }}
          body: "${{ needs.release-prerequisites.outputs.RELEASE_BODY }}"
          prerelease: false
          fail_on_unmatched_files: true
          files: |
            ./*

  build-docker-image:
    needs:
      - golang-build
      - verify-version
      - release-prerequisites
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: artifact_webscan_linux_*
          path: ./artifacts
          merge-multiple: true

      - name: Make binaries executable
        shell: bash
        run: |
          chmod +x ./artifacts/webscan_linux_*

      - uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4

      - uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6
        with:
          images: ghcr.io/${{ github.repository }}
          # value= for the same reason as tag_name above: semver parsing defaults
          # to github.ref, which on the workflow_call path is a branch. Without
          # it the image gets nothing but :latest, and Flux image-automation on
          # hydra - which scans the X.Y.Z tags - would never see the release.
          tags: |
            type=semver,pattern={{version}},value=${{ env.TAG }}
            type=semver,pattern={{major}}.{{minor}},value=${{ env.TAG }}
            type=semver,pattern={{major}},value=${{ env.TAG }}
            type=raw,value=latest

      - name: Build and push Docker image
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: VERSION=${{ env.TAG }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  trigger-external-updates:
    needs: release-artifacts
    runs-on: ubuntu-latest
    steps:
      - run: |
          curl "https://goreportcard.com/checks" \
            -X POST \
            -H "Content-Type: application/x-www-form-urlencoded" \
            -d "repo=${{ github.repository }}"

      - name: Trigger homebrew tap update workflow
        env:
          EXTERNAL_GITHUB_TOKEN: ${{ secrets.HOMEBREW_TAP_ACCESS_TOKEN }}
        run: |
          unset GITHUB_TOKEN
          echo "$EXTERNAL_GITHUB_TOKEN" | gh auth login --with-token
          gh workflow run update-webscan.yml \
            --repo thetillhoff/homebrew-tap
